# Revision history for datastar-hs-zstd

## 1.0.0.0 -- 2026-07-04

* First release. `Hypermedia.Datastar.Compression.Zstd`, split out of
  `datastar-hs` 1.0.x (where it was behind the `zstd` cabal flag). Not yet
  published to Hackage: waiting on an hs-zstd release with the streaming
  `flushStream` FFI
  ([#3](https://github.com/starfederation/datastar-haskell/issues/3)).
